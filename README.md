# Just a test project with Docker and Docker Compose

I used 2 docker images:
1. A small application allowing to manipulate data in a table via http requests. It is based on golang:1.26-bookworm.
2. PostgreSQL (postgres:18-bookworm).

Docker Compose was used for easier maintenance of this multi-container Docker application.
